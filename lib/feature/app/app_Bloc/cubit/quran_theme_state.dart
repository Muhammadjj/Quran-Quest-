part of 'quran_theme_cubit.dart';

class ThemeState {
  ThemeState({required this.themeMode});
  final ThemeMode themeMode;

  // ThemeState copyWith({ThemeMode? themeMode}) {
  //   return ThemeState(themeMode: themeMode ?? this.themeMode);
  // }
}
